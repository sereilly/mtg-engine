[pytest]
testpaths = tests
addopts = -q
markers =
    slow: long AI-simulation batch tests (deselect with -m "not slow")
    cr(*rules): Comprehensive Rules citation(s) the test verifies, e.g. cr("508.1a"); collected by scripts/rules_progress.py
