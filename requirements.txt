# Runtime and test dependencies for the engine and web app.
# Pinned to the versions the workspace venv runs, so CI reproduces local results.
# Install: python -m pip install -r requirements.txt

fastapi==0.136.3
starlette==1.1.0
pydantic==2.13.4
uvicorn==0.47.0

# httpx backs fastapi.testclient.TestClient, which the web-layer tests use.
httpx==0.28.1
pytest==9.0.3

# Local parallel runs only: `python -m pytest -n auto` for the merge-gate loop.
# Deliberately NOT in pytest.ini's addopts — CI's timed run must stay serial or
# the suite-time BASELINE/BUDGET readings in .github/workflows/ci.yml lose
# their meaning.
pytest-xdist==3.8.0
execnet==2.1.2
